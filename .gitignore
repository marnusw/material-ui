node_modules
npm-debug.log
build
coverage

# Exclude compiled files
#lib -- Add the lib to git so it can be pulled via npm until switching back to the main library
icon-builder/js
icon-builder/jsx

# Exclude IDE project folders
.idea

# OS files
.DS_STORE
